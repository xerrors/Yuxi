# Yuxi

Yuxi 是面向企业的知识库与智能体平台。本文件只登记本项目特有的领域术语，用于统一代码、文档和讨论中的用词，不描述实现。

## Language

### 组织

**组织节点**：
组织机构树上的一个单元，可以是集团、分子公司或部门。三者是同一概念的不同层级，不是不同实体。
_Avoid_: 部门（仅指最末层时才用）、机构、单位、组织架构

**集团根**：
组织机构树唯一的根节点，所有其他组织节点都是它的后代。
_Avoid_: 默认部门、顶级部门、总部

**节点类型**：
组织节点上用于界面展示的标签（集团 / 分子公司 / 部门），不参与任何权限判定或结构校验。

**子树**：
一个组织节点自身及其全部后代节点的集合。资源授权的最小表达单位。

**归属节点**：
一个用户所属的唯一组织节点。用户只能归属一个节点，且可以归属树上任意层级，不限于叶子。
_Avoid_: 用户部门、所在部门

### 权限

**角色**：
一组功能权限的命名集合，可分配给用户。每个有效用户至少拥有一个角色；一个用户可以拥有多个普通角色，其有效功能权限为各角色权限的并集，`superadmin` 独占且不与其他角色混配。

**内置角色 / 自定义角色**：
`superadmin`、`admin`、`user` 是不可删除、不可改标识且权限只读的内置角色；自定义角色按业务职责创建。角色之间不继承，可复制已有角色后调整。

**功能权限**：
允许角色执行某类业务操作的授权，使用“资源 + 操作”表达，例如查看用户、创建用户或修改系统配置。菜单和按钮是否展示只是功能权限的界面投影，不是最终授权边界。
_Avoid_: 菜单权限、按钮权限

**基础聊天能力**：
所有登录用户都可以新建会话并使用共享范围允许访问的智能体，不进入角色权限目录；智能体创建、编辑、删除和配置仍由 `agent:manage` 控制。

**权限目录**：
由服务端代码维护的稳定功能权限清单，权限标识采用 `resource:action`。数据库只保存角色与目录中权限标识的关联，不允许通过管理界面创建新的权限标识。

**数据范围**：
用户执行有组织归属的数据查询或操作时，允许覆盖的组织节点集合。选择一个组织节点默认表示该节点自身及其全部后代；只有明确称为“直属”时才精确匹配单个节点。
_Avoid_: 数据权限、部门权限

**数据范围类型**：
数据范围固定分为无数据、仅本人、本组织及下级、指定组织及下级、全部数据五种。系统不提供可配置的“仅直属组织”类型。

**角色默认数据范围 / 用户角色范围**：
角色定义默认数据范围；向用户分配角色时，可以继承默认值或为这一次分配设置更窄的个性化覆盖。不存在脱离角色分配而单独生效的用户数据范围，扩大范围必须通过另一条角色分配完成。
_Avoid_: 用户全局数据权限

**有效授权**：
同一条用户角色分配同时命中功能权限和数据范围时形成的最终授权；不同角色的功能权限和数据范围不能交叉拼接。系统默认拒绝，不支持显式拒绝；任意一条完整分配命中即可允许。

**请求级授权上下文**：
服务端在一次请求内加载并复用的当前用户角色、功能权限和数据范围。它不跨请求缓存，角色、组织或范围变化从下一次请求起生效。

**组织快照**：
业务事件发生时记录的用户归属节点，用于 Dashboard、审计和历史报表。它不随用户调动或组织树调整而改写。

**数据所有者**：
业务数据中明确记录的创建人、负责人或用户本人。数据范围为“仅本人”时只按这些明确字段判断；没有所有者字段的数据不做推断，视为不在范围内。

**安全审计**：
角色、功能权限、默认数据范围和用户角色分配发生变化时保存的不可变操作记录，至少包含操作者、操作时间、目标以及变更前后值。授予或撤销 `superadmin` 时还必须记录原因。

**共享范围**：
一条授权规则，声明一份资源对哪些人可见。取值为全局、组织节点集合或用户集合三者之一。
_Avoid_: 权限范围、可见范围、scope（中文语境下）

**全局共享**：
不参照组织机构树的共享范围，任何登录用户可见，包含归属集团根的用户。它与"共享给集团根"在效果上几乎等价，但意图不同：前者是"与组织无关"，后者是"明确面向全集团"。

**子树继承**：
共享范围选中某个组织节点时，该节点子树内的全部用户都获得该权限。这是组织节点参与权限判定的唯一方式。
_Avoid_: 权限下发、权限传递

**读取范围 / 管理范围**：
资源共享范围的两个独立维度。管理范围必须被读取范围覆盖：每个管理节点都必须是某个读取节点自身或其后代。

**管理域**：
管理员处理用户等组织数据时适用的数据范围，与资源的共享范围相互独立。`superadmin` 管理整棵树；归属集团根的 `admin` 管理整棵树，其他 `admin` 管理其归属节点子树。
_Avoid_: 共享范围、资源管理范围

### 智能体运行

**运行请求（Run Request）**：
用户或外部通道提交、等待派发的一次智能体请求；`request_id` 是它的幂等标识。派发后由一个运行承接执行。
_Avoid_: Task、Job（容易与通用后台 Tasker 混淆）

**运行（Agent Run）**：
一次已派发的智能体业务执行，拥有面向用户的状态、输入与最终输出；一次运行可以包含多个串行运行尝试。
_Avoid_: 运行请求、运行尝试

**运行尝试（Run Attempt）**：
一个执行 Owner 对某次运行的单次占有事实；它记录开始、续租与终止结果，不代表新的用户请求。
_Avoid_: Run、Retry

**运行清单（Run Manifest）**：
一次运行实际采用的 Agent、模型、工具与 Skill 版本的不可变摘要。
_Avoid_: 当前配置、Runtime Config

**沙盒作用域（Sandbox Scope）**：
用于隔离一组用户文件与 Skill 的执行身份，不等同于运行或运行尝试。
_Avoid_: Run sandbox、每任务沙盒

### 外部身份

**OA 账号身份**：
由公司编码和 OA `account` 共同确定的外部用户身份，是 Yuxi 映射本地普通用户的稳定键。
_Avoid_: OA 用户名、登录名

**账号代入登录**：
父项目将其已认证用户的 OA `account` 交给 Yuxi，由 Yuxi 换取 OA 凭证并校验 OA 用户信息后建立本地登录态的内网试用方式。它不是可用于生产环境的独立单点登录。
_Avoid_: SSO、免认证登录
